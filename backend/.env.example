NODE_ENV=development
PORT=5000
JWT_SECRET=replace-with-a-long-random-secret
JWT_EXPIRES_IN=8h
FRONTEND_ORIGINS=http://localhost:5173

# Optional first-agent provisioning for a new data file
# BOOTSTRAP_AGENT_EMAIL=agent@example.com
# BOOTSTRAP_AGENT_NAME=IT Agent
# BOOTSTRAP_AGENT_PASSWORD=replace-with-a-long-password

# Password reset email settings
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM=support@sayedfarms.com
